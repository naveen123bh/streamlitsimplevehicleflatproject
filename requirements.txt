# Streamlit and UI
streamlit==1.38.0
rich<14  # Streamlit-compatible version
streamlit-webrtc==0.63.4

# Whisper / ASR
openai-whisper==20230424

# PyTorch CPU-only (compatible with Python 3.12)
torch==2.2.0+cpu
torchaudio==2.2.0+cpu

# Audio processing
wavio
numpy
pytz

# Rich dependencies (installed automatically)
markdown-it-py>=2.2.0
mdurl~=0.1
pygments<3.0.0,>=2.13.0

# Optional: temporary file handling, usually in stdlib
tempfile
